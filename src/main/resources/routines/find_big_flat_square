create function find_big_flat_square(rooms_criteria integer) returns integer
    language plpgsql
as
$$
declare
    itemSquare integer;
begin
    select max(square)
    into itemSquare
    from real_estates
    where type = 'flat' and rooms = rooms_criteria;
    return itemSquare;
end
$$;

alter function find_big_flat_square(integer) owner to postgres;

